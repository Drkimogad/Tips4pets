// Set default values for editable fields
document.getElementById("email").textContent = "contact@socialgrow.com";
document.getElementById("whatsapp").textContent = "+123456789";

// Placeholder image
document.getElementById("hero-image").src = "https://via.placeholder.com/150";

// Optionally, add logic to update email, WhatsApp, and hero image dynamically
// Example: Replace content dynamically (if admin system is added in the future)
